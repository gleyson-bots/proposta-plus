# Desenvolvimento local — Prisma + SQLite + better-sqlite3
DATABASE_URL="file:./prisma/dev.db"

# Na Vercel nenhuma variável de banco é necessária.
# O app usa automaticamente file:/tmp/proposta-plus.db para homologação.
